# 07 版本路线图（Git 里程碑）

> 每个里程碑 = 一次可编译/可验证的 Git 提交（tag: vX.Y）。
> 原则：先协议后外设，先从站后主站，先中断后 DMA，先功能后优化。

| 版本 | 主题                 | 交付物                                                       | 验证方式                 |
|------|----------------------|--------------------------------------------------------------|--------------------------|
| v0.1 | 工程初始化           | 例程上移、目录骨架、设计文档、README、.gitignore             | Keil 编译 LED 例程       |
| v0.2 | UART/RS485 驱动      | `Common/types.h`、`BSP/bsp_board_cfg.h`、`bsp_uart.c/h`、`bsp_rs485.c/h`（DE 可选+TC 等待）、环回测试入口 | 板级自发自收/串口回显 |
| v0.3 | Modbus CRC16         | `Protocol/modbus_crc.c/h`（查表+逐位）、PC 单测向量          | gcc 单测 / Python 比对  |
| v0.4 | Slave 0x03           | `Protocol/modbus_slave.c/h`（帧状态机）、`modbus_register` 表、中断收帧 | PC 模拟 + 双板读保持寄存器 |
| v0.5 | Slave 0x01/0x06      | 线圈区读写、单寄存器写、写后参数同步                         | Python 工具读写验证     |
| v0.6 | 异常响应             | 0x01/0x02/0x03 异常码、帧校验丢弃、CRC 计数、日志钩子        | 异常注入测试 E1~E8      |
| v0.7 | Master 实现          | `Protocol/modbus_master.c/h`、任务表、`Master/master_main.c` | A↔B 轮询读数            |
| v0.8 | 超时/重试/统计       | 超时定时、重试队列、统计对象、故障状态机                     | E9/E10 断电与断线测试   |
| v0.9 | 24C02 参数保存       | `BSP/bsp_eeprom.c`、`Storage/config.c/h`（版本+CRC+默认值）  | P1/P2/P3                |
| v1.0 | LCD 状态显示         | `BSP/bsp_lcd.c`、按键驱动、LCD 页面（首页/配置/错误）        | 目检 + 按键操作         |
| v1.1 | W25Q128 日志         | `BSP/bsp_w25q128.c`、`Storage/log.c/h`（环形区、时间戳）     | P4 + 事件注入           |
| v1.2 | Python 测试工具      | `Tools/python/`(crc/master/logger/tool)                      | N 系列 + monitor        |
| v1.3 | 压力/稳定性测试      | pressure/monitor 增强、结果存档                               | 10000 帧 + 1 小时       |
| v1.4 | DMA + RingBuffer     | `bsp_uart` DMA 收发、`Common/ring_buffer`、空闲中断/DMA 半满处理 | 全回归 + 长期稳定性     |
| v2.0 | 最终整合             | README 最终版、验收清单全绿、代码清理、版本 tag v2.0         | §8 验收清单             |

## 提交规范

- 每次提交只做一件事；消息格式：`vX.Y: 简述`（中文描述亦可）。
- 每版本至少一次可编译状态；协议/公共代码尽量带 PC 侧验证。
- 硬件相关的"待确认项"未定前以默认配置提交并在文档标注。

## 当前进度

| 版本 | 状态 | 日期       | 备注 |
|------|------|-----------|------|
| v0.1 | ✅ 完成 | 本次会话 | 例程基线 + 骨架 + 文档 |
| v0.2 | ✅ 完成 | 本次会话 | USART3 寄存器级驱动 + RS485 DE 可选 + TC 等待 + 双角色回环 demo |
| v0.3 | 进行中 | —         | 待实现 |
